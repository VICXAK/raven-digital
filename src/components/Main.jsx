import React from "react";

import "../scss/components/Main.scss";

const Main = ({ children }) => {
  return (
    <main className="main">
      
      {children}
    
    </main>
  );
};

export default Main;
